import IhsanCore
import IhsanDesignSystem
import SwiftUI

/// A prayer's ornament for the Lock Screen and the Dynamic Island.
///
/// The drawing is `OrnamentLinework` in the design system — one
/// definition, so a lock-screen ornament and an in-app one can never
/// disagree about what Maghrib looks like.
typealias LockOrnament = OrnamentLinework
